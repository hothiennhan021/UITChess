<Window x:Class="ChessUI.ChangeAvatarDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Width="420"
        Height="320"
        Background="#00000000"
        WindowStyle="None"
        AllowsTransparency="True">

    <Window.Resources>

        <Storyboard x:Key="FadeInAnimation">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="0" To="1"
                             Duration="0:0:0.25" />
            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)"
                             From="0.85" To="1"
                             Duration="0:0:0.25" />
            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)"
                             From="0.85" To="1"
                             Duration="0:0:0.25" />
        </Storyboard>

        <Style x:Key="RoundedButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="12"
                                Padding="10"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" 
                                        Value="#6ab0ff"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" 
                                        Value="#2f85ff"/>
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.96" ScaleY="0.96"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>

                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid Opacity="0" RenderTransformOrigin="0.5,0.5">
        <Grid.RenderTransform>
            <ScaleTransform ScaleX="1" ScaleY="1"/>
        </Grid.RenderTransform>

        <Grid.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard Storyboard="{StaticResource FadeInAnimation}"/>
            </EventTrigger>
        </Grid.Triggers>

        <Border Background="#AA000000" CornerRadius="20" Padding="2">
            <Border.Effect>
                <DropShadowEffect Color="Black"
                                  BlurRadius="18"
                                  ShadowDepth="0"
                                  Opacity="0.7"/>
            </Border.Effect>

            <Border CornerRadius="18" BorderBrush="#4da3ff" BorderThickness="3" Background="#111111">
                <StackPanel Margin="20" HorizontalAlignment="Center">

                    <TextBlock Text="Chọn kiểu avatar"
                               FontSize="26"
                               FontWeight="Bold"
                               Foreground="#4da3ff"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,20"/>

                    <Button Content="🎨  Avatar Có Sẵn"
                            Width="260"
                            Height="50"
                            FontSize="18"
                            Background="#3A7BFF"
                            Style="{StaticResource RoundedButton}"
                            Click="DefaultAvatar_Click"
                            Margin="0,0,0,10"/>

                    <Button Content="📁  Tải Avatar Từ Máy"
                            Width="260"
                            Height="50"
                            FontSize="18"
                            Background="#4CAF50"
                            Style="{StaticResource RoundedButton}"
                            Click="UploadAvatar_Click"
                            Margin="0,0,0,15"/>

                    <Button Content="Hủy"
                            Width="200"
                            Height="45"
                            FontSize="18"
                            Background="#555555"
                            Style="{StaticResource RoundedButton}"
                            Click="Cancel_Click"/>
                </StackPanel>
            </Border>

        </Border>
    </Grid>
</Window>