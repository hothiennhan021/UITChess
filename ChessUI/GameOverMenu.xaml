<UserControl x:Class="ChessUI.GameOverMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="500">

    <UserControl.Resources>
        <Style TargetType="Button" x:Key="MenuButton">
            <Setter Property="Background" Value="#444"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5" BorderBrush="Gray" BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#666"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="#AA000000">
        <Border Background="#252525" BorderBrush="#FFD700" BorderThickness="2" CornerRadius="10" 
                HorizontalAlignment="Center" VerticalAlignment="Center" 
                Padding="20" MinWidth="300">
            <StackPanel>
                <TextBlock x:Name="WinnerText" Text="KẾT THÚC" Foreground="White" FontSize="28" FontWeight="Bold" 
                           HorizontalAlignment="Center" Margin="0,0,0,10"/>

                <TextBlock x:Name="ReasonText" Text="Lý do kết thúc" Foreground="LightGray" FontSize="16" 
                           HorizontalAlignment="Center" Margin="0,0,0,20" TextWrapping="Wrap"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button x:Name="BtnRestart" Content="CHƠI LẠI" Style="{StaticResource MenuButton}" Click="Restart_Click" Width="100"/>
                    <Button x:Name="BtnAnalyze" Content="PHÂN TÍCH" Style="{StaticResource MenuButton}" Click="Analyze_Click" Width="100"/>
                    <Button Content="THOÁT" Style="{StaticResource MenuButton}" Click="Exit_Click" Width="100"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>