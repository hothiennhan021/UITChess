<Window x:Class="ChessUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChessUI"
        mc:Ignorable="d"
        Title="Chess Online"
        Closing="Window_Closing"
        WindowStartupLocation="CenterScreen"
        Background="#202020"
        Width="1100" Height="850" MinWidth="900" MinHeight="700" MaxWidth="1200" MaxHeight="950">

    <Window.Resources>
        <SolidColorBrush x:Key="StrokeColor">#260000</SolidColorBrush>
        <SolidColorBrush x:Key="FillColor">#cc380404</SolidColorBrush>
        <SolidColorBrush x:Key="TextColor">#ffa274</SolidColorBrush>
        <SolidColorBrush x:Key="ButtonColor">#730909</SolidColorBrush>

        <Style TargetType="Border" x:Key="BorderStyle">
            <Setter Property="BorderThickness" Value="4"/>
            <Setter Property="BorderBrush" Value="{StaticResource StrokeColor}"/>
            <Setter Property="Background" Value="{StaticResource FillColor}"/>
            <Setter Property="CornerRadius" Value="10"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="TextStyle">
            <Setter Property="FontFamily" Value="Segoe UI Light"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
        </Style>

        <Style TargetType="Button" x:Key="ButtonStyle">
            <Setter Property="FontFamily" Value="Segoe UI Light"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Background" Value="{StaticResource StrokeColor}"/>
            <Setter Property="BorderBrush" Value="{StaticResource ButtonColor}"/>
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5"/>
                </Style>
            </Style.Resources>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="11*"/>
            <ColumnDefinition Width="54*"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="0,0,10,0" Grid.ColumnSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#303030" CornerRadius="5" Padding="10" Margin="0,0,0,5">
                <Grid>
                    <StackPanel HorizontalAlignment="Left" VerticalAlignment="Center">
                        <TextBlock x:Name="lblBlackPlayerName" Text="Opponent" Foreground="White" FontWeight="Bold" FontSize="16"/>
                        <TextBlock x:Name="lblBlackElo" Foreground="Gray" FontSize="12"/>
                    </StackPanel>
                    <Border Background="Black" CornerRadius="3" Padding="10,5" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <TextBlock x:Name="lblBlackTime" Text="05:00" Foreground="White" FontSize="24" FontFamily="Consolas" FontWeight="Bold"/>
                    </Border>
                </Grid>
            </Border>

            <Viewbox Grid.Row="1">
                <Grid x:Name="BoardGrid" Width="600" Height="600" MouseDown="BoardGrid_MouseDown">
                    <Grid.Background>
                        <SolidColorBrush Color="#202020"/>
                    </Grid.Background>
                    <UniformGrid x:Name="HighlightGrid" Rows="8" Columns="8"/>
                    <UniformGrid x:Name="PieceGrid" Rows="8" Columns="8" RenderOptions.BitmapScalingMode="HighQuality"/>
                    <Canvas x:Name="ArrowCanvas" IsHitTestVisible="False" Width="600" Height="600"/>
                </Grid>
            </Viewbox>

            <Border Grid.Row="2" Background="#303030" CornerRadius="5" Padding="10" Margin="0,5,0,0">
                <Grid>
                    <StackPanel HorizontalAlignment="Left" VerticalAlignment="Center">
                        <TextBlock x:Name="lblWhitePlayerName" Text="You" Foreground="White" FontWeight="Bold" FontSize="16"/>
                        <TextBlock x:Name="lblWhiteElo" Foreground="Gray" FontSize="12"/>
                    </StackPanel>
                    <Border Background="Black" CornerRadius="3" Padding="10,5" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <TextBlock x:Name="lblWhiteTime" Text="05:00" Foreground="White" FontSize="24" FontFamily="Consolas" FontWeight="Bold"/>
                    </Border>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Column="2" Background="#252525" CornerRadius="5" BorderBrush="#404040" BorderThickness="1">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Phòng Chat" Foreground="LightGray" FontWeight="Bold" Margin="0,0,0,10"/>

                <RichTextBox x:Name="txtChatHistory" Grid.Row="1" Background="#151515" Foreground="White" BorderThickness="0" IsReadOnly="True" VerticalScrollBarVisibility="Auto" Margin="0,0,0,10">
                    <FlowDocument>
                        <Paragraph>
                            <Run Text="Hệ thống: " Foreground="Yellow"/>
                            <Run Text="Chào mừng bạn đến phòng chơi!" Foreground="Gray"/>
                        </Paragraph>
                    </FlowDocument>
                </RichTextBox>

                <Border x:Name="DrawRequestBorder" Grid.Row="1" VerticalAlignment="Bottom" Background="#E6222222" BorderBrush="#FFD700" BorderThickness="1" CornerRadius="5" Padding="10" Margin="5" Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock x:Name="lblDrawTimer" Text="Đối thủ xin hòa (15s)" Foreground="White" HorizontalAlignment="Center" FontWeight="Bold" Margin="0,0,0,5"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Button Content="Đồng ý" Width="70" Background="#4CAF50" Foreground="White" Margin="5" Click="BtnAcceptDraw_Click" Cursor="Hand">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="3">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                            <Button Content="Từ chối" Width="70" Background="#F44336" Foreground="White" Margin="5" Click="BtnDeclineDraw_Click" Cursor="Hand">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="3">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Grid Grid.Row="2" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="btnDraw" Grid.Column="0" Content="Xin Hòa" Height="30" Margin="0,0,5,0" Background="#444" Foreground="White" BorderThickness="0" Click="btnDraw_Click" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="3" BorderBrush="#666" BorderThickness="1">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Button x:Name="btnResign" Grid.Column="1" Content="Đầu hàng" Height="30" Margin="5,0,0,0" Background="#800" Foreground="White" BorderThickness="0" Click="btnResign_Click" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="3" BorderBrush="#A00" BorderThickness="1">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>

                <Grid Grid.Row="3">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="txtChatInput" Grid.Column="0" Height="30" Padding="5" Background="#303030" Foreground="White" BorderThickness="0" VerticalContentAlignment="Center" KeyDown="txtChatInput_KeyDown"/>
                    <Button x:Name="btnSendChat" Grid.Column="1" Content="Gửi" Width="50" Height="30" Margin="5,0,0,0" Background="#404040" Foreground="White" BorderThickness="0" Click="btnSendChat_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="3">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Grid>
        </Border>

        <ContentControl x:Name="MenuContainer"
                        Grid.ColumnSpan="3"
                        Grid.RowSpan="3"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        Panel.ZIndex="100"/>

        <local:GameOverMenu x:Name="MenuOverlay" 
                            Grid.RowSpan="3" 
                            Grid.ColumnSpan="3"
                            Visibility="Collapsed" 
                            HorizontalAlignment="Center" 
                            VerticalAlignment="Center"
                            Panel.ZIndex="99"/>
    </Grid>
</Window>